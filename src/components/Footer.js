import React from 'react';
import { Navbar, Nav } from 'react-bootstrap';
function Footer(props) {
  return (
    <Navbar fixed='bottom' className='text-center'>
      <Nav.Item>Footer</Nav.Item>
    </Navbar>
  );
}

export default Footer;
